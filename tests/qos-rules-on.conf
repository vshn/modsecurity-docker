# mod_qos test rules (server scope).
# Mark /blockme as a QS_Limit event; deny a client IP after 3 such events
# within 60s. Denied at request level -> HTTP 500 by default.
SetEnvIf Request_URI ^/blockme QS_Limit=1
QS_ClientEventLimitCount 3 60