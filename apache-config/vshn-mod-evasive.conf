# mod_evasive (jvdmr fork) — load unconditionally at main scope; inert unless
# DOSEnabled is set to true inside the VirtualHost (see conf/extra/evasive.conf).
LoadModule evasive_module /usr/local/apache2/modules/mod_evasive.so